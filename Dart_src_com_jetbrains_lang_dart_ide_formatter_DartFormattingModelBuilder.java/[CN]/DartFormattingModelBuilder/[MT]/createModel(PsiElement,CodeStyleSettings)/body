{
  final PsiFile psiFile=element.getContainingFile();
  return FormattingModelProvider.createFormattingModelForPsiFile(psiFile,new DartBlock(psiFile instanceof DartFile ? psiFile.getNode() : element.getNode(),null,null,settings),settings);
}
