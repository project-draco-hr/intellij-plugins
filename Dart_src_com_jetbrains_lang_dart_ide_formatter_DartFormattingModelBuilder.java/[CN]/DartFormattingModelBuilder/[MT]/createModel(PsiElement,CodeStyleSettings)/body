{
  final PsiFile psiFile=element.getContainingFile();
  return new DartFormattingModel(psiFile,settings,new DartBlock(psiFile instanceof DartFile ? psiFile.getNode() : element.getNode(),null,null,settings));
}
