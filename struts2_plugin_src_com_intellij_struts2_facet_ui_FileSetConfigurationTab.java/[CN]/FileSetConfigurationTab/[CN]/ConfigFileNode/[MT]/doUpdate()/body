{
  final VirtualFile file=myFilePointer.getFile();
  if (file != null) {
    renderFile(file,getPlainAttributes(),SimpleTextAttributes.GRAYED_ATTRIBUTES,null);
  }
 else {
    renderFile(file,getErrorAttributes(),getErrorAttributes(),StrutsBundle.message("facet.fileset.filenotfound"));
  }
}
