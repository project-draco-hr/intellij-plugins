{
  if ((data[offset++] & 0xFF) < 128) {
    return offset;
  }
  if ((data[offset++] & 0xFF) < 128) {
    return offset;
  }
  if ((data[offset++] & 0xFF) < 128) {
    return offset;
  }
  return offset + 1;
}
