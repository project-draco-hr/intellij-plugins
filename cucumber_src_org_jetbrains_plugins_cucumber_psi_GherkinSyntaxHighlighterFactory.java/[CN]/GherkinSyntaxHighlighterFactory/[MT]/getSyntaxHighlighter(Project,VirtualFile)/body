{
  final GherkinKeywordProvider keywordProvider=project != null ? CucumberLanguageService.getInstance(project).getKeywordProvider() : new PlainGherkinKeywordProvider();
  return new GherkinSyntaxHighlighter(keywordProvider);
}
