{
  if (obj instanceof SearchResult) {
    SearchResult other=(SearchResult)obj;
    return ObjectUtilities.equals(other.location,location) && ObjectUtilities.equals(other.kind,kind) && other.isPotential == isPotential && ObjectUtilities.equals(other.path,path);
  }
  return false;
}
