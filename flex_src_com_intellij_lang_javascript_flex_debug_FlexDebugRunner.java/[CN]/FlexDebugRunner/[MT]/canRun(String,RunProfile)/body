{
  return DefaultDebugExecutor.EXECUTOR_ID.equals(executorId) && (profile instanceof FlexRunConfiguration || profile instanceof FlexIdeRunConfiguration || profile instanceof NewFlexUnitRunConfiguration|| profile instanceof RemoteFlashRunConfiguration);
}
