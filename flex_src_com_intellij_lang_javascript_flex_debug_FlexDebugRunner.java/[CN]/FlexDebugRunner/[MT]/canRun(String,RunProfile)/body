{
  return DefaultDebugExecutor.EXECUTOR_ID.equals(executorId) && (profile instanceof FlexRunConfiguration || profile instanceof FlashRunConfiguration || profile instanceof NewFlexUnitRunConfiguration|| profile instanceof RemoteFlashRunConfiguration);
}
