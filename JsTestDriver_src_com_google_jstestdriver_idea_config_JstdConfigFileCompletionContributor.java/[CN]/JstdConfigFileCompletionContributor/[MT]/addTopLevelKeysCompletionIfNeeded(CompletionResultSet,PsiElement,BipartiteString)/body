{
  YAMLDocument document=JstdConfigFileUtils.getVerifiedHierarchyHead(element.getParent(),new Class[]{YAMLKeyValue.class},YAMLDocument.class);
  if (document == null) {
    document=JstdConfigFileUtils.getVerifiedHierarchyHead(element.getParent(),new Class[]{},YAMLDocument.class);
  }
  if (document != null) {
    addTopLevelKeysCompletions(result,caretBipartiteElementText.getPrefix());
  }
}
