{
  if (obj instanceof ExtractLocalVariableOptions) {
    ExtractLocalVariableOptions other=(ExtractLocalVariableOptions)obj;
    return ObjectUtilities.equals(other.name,name) && other.extractAll == extractAll;
  }
  return false;
}
