{
  myFactory=new ConfigurationFactory(this){
    public RunConfiguration createTemplateConfiguration(    Project project){
      return PlatformUtils.isFlexIde() ? new NewFlexUnitRunConfiguration(project,this,"") : new FlexUnitRunConfiguration(project,this,"");
    }
  }
;
}
