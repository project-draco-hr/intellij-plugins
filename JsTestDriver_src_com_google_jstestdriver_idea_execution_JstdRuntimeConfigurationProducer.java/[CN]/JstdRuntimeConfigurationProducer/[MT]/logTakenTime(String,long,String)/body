{
  long endTimeNano=System.nanoTime();
  String message=String.format("[JsTD] Time taken by '" + actionName + "': %.2f ms, extra args: %s\n",(endTimeNano - startTimeNano) / 1000000.0,Arrays.toString(args));
  LOG.info(message);
}
