{
  if (config instanceof DartCoverageEnabledConfiguration) {
    DartCoverageEnabledConfiguration dartCoverageEnabledConfiguration=(DartCoverageEnabledConfiguration)config;
    Project project=config.getConfiguration().getProject();
    try {
      VirtualFile contextFile=((DartCommandLineRunConfiguration)dartCoverageEnabledConfiguration.getConfiguration()).getRunnerParameters().getDartFile();
      return new DartCoverageSuite(covRunner,name,coverageDataFileProvider,new Date().getTime(),false,false,false,project,this,contextFile,dartCoverageEnabledConfiguration.getCoverageProcess());
    }
 catch (    RuntimeConfigurationError e) {
      LOG.warn(e);
    }
  }
  return null;
}
