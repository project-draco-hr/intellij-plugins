{
  final String dartFlags=envVars.get(DART_FLAGS_ENV_VAR);
  return dartFlags != null && (dartFlags.trim().equals(CHECKED_MODE_OPTION) || dartFlags.startsWith(CHECKED_MODE_OPTION + " ") || dartFlags.endsWith(" " + CHECKED_MODE_OPTION)|| dartFlags.contains(" " + CHECKED_MODE_OPTION + " "));
}
