{
  final VirtualFile folder=createFolderIfMissing(project,templateFolderPath);
  return folder != null && checkIfEmpty(project,folder) && doCreateWrapper(project,sdk,folder,enableHistory,checkPlayerVersion,expressInstall);
}
