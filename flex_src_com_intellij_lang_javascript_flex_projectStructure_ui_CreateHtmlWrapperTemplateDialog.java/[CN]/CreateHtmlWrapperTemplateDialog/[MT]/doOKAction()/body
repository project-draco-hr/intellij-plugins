{
  if (createHtmlWrapperTemplate(myProject,mySdk,myWrapperFolderComponent.getComponent().getText().trim(),myEnableHistoryCheckBox.isSelected(),myCheckPlayerVersionCheckBox.isSelected(),myExpressInstallCheckBox.isEnabled() && myExpressInstallCheckBox.isSelected())) {
    super.doOKAction();
  }
}
