{
  final String scriptFilePath=task.getInputFilePath();
  return !StringUtil.isEmpty(scriptFilePath) && VirtualFileManager.getInstance().findFileByUrl(VfsUtilCore.pathToUrl(scriptFilePath)) != null;
}
