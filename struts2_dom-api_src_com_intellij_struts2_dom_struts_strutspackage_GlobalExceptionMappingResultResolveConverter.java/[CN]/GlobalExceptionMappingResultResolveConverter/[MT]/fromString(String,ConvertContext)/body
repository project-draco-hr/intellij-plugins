{
  if (value == null) {
    return null;
  }
  for (  final GlobalResult result : getVariants(context)) {
    if (value.equals(result.getName().getStringValue())) {
      return result;
    }
  }
  return null;
}
