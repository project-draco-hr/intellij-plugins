{
  if (!myDelegate.contains(file)) {
    return false;
  }
  FileType type=file.getFileType();
  return type == StdFileTypes.HTML || type == StdFileTypes.XHTML || type == StdFileTypes.JSP || type == StdFileTypes.JSPX || type == StdFileTypes.XML;
}
