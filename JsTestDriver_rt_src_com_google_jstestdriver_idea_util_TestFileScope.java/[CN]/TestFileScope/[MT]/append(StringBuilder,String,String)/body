{
  if (buf.length() > 0) {
    buf.append(",");
  }
  buf.append('^').append(testCaseName);
  if (testMethodName != null) {
    buf.append(".");
    buf.append(testMethodName);
  }
  buf.append('$');
}
