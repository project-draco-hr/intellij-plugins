{
  return descriptor instanceof ClassBackedElementDescriptor && JSCommonTypeNames.ARRAY_CLASS_NAME.equals(descriptor.getName()) && JavaScriptSupportLoader.MXML_URI3.equals(((ClassBackedElementDescriptor)descriptor).context.namespace);
}
