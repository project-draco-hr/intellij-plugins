{
  return !ContainerUtil.process(bcs,new Processor<FlexIdeBuildConfiguration>(){
    @Override public boolean process(    FlexIdeBuildConfiguration configuration){
      return !dependsOnSdk(configuration,sdk);
    }
  }
);
}
