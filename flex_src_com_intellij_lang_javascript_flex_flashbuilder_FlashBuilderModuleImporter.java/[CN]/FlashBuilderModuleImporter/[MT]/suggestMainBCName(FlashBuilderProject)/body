{
  return FlexBuildConfiguration.APPLICATION.equals(fbProject.getCompilerOutputType()) && !fbProject.getMainAppClassName().isEmpty() ? StringUtil.getShortName(fbProject.getMainAppClassName()) : fbProject.getName();
}
