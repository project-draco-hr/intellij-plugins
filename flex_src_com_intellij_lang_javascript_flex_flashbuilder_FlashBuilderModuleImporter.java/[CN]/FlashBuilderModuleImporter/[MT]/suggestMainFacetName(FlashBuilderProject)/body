{
  final String type=flashBuilderProject.getCompilerOutputType();
  return flashBuilderProject.getProjectType().toString() + " " + type+ (FlexBuildConfiguration.APPLICATION.equals(type) && flashBuilderProject.getApplicationClassNames().size() > 0 ? " 1" : "");
}
