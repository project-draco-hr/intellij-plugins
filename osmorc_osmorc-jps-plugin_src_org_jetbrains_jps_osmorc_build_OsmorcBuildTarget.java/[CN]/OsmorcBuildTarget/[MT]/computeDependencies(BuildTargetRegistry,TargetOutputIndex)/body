{
  Collection<ModuleBasedTarget<?>> targets=targetRegistry.getModuleBasedTargets(getModule(),BuildTargetRegistry.ModuleTargetSelector.PRODUCTION);
  return Collections.<BuildTarget<?>>unmodifiableCollection(targets);
}
