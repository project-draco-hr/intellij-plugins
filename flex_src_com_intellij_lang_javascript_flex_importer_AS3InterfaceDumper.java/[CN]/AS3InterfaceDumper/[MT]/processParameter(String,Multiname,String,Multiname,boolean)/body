{
  if (rest) {
    append("... ");
    append(name);
  }
 else {
    append(name);
    append(":");
    dumpTypeRef(type,parentName,true);
    if (value != null) {
      processValue(type,value);
    }
  }
}
