{
  List<Type> types=ContainerUtil.filter(Type.values(),new Condition<Type>(){
    @Override public boolean value(    Type type){
      return type.isAvailable();
    }
  }
);
  return types.toArray(new Type[types.size()]);
}
