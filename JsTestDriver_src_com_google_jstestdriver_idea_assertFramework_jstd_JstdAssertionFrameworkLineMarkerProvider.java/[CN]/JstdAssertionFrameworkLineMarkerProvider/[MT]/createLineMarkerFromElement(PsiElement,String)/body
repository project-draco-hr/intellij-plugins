{
  return new LineMarkerInfo<PsiElement>(testElement,testElement.getTextRange(),AllIcons.General.Run,Pass.UPDATE_ALL,new Function<PsiElement,String>(){
    @Override public String fun(    PsiElement element){
      return "Run '" + displayName + "'";
    }
  }
,new GutterIconNavigationHandler<PsiElement>(){
    @Override public void navigate(    MouseEvent e,    PsiElement elt){
      if (elt.isValid()) {
        run(elt);
      }
    }
  }
,GutterIconRenderer.Alignment.CENTER);
}
