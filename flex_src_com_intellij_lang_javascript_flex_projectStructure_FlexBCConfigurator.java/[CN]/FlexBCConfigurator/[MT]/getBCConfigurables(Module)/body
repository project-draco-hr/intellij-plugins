{
  return ContainerUtil.map(myConfigEditor.getConfigurations(module),new Function<ModifiableFlexBuildConfiguration,CompositeConfigurable>(){
    @Override public CompositeConfigurable fun(    ModifiableFlexBuildConfiguration configuration){
      return myConfigurablesMap.get(configuration);
    }
  }
);
}
