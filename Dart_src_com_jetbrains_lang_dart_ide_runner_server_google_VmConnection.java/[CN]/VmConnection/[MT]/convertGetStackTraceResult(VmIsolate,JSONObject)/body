{
  VmResult<List<VmCallFrame>> result=VmResult.createFrom(object);
  if (object.has("result")) {
    List<VmCallFrame> frames=VmCallFrame.createFrom(isolate,object.getJSONObject("result").getJSONArray("callFrames"));
    result.setResult(frames);
  }
  return result;
}
