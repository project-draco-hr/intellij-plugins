{
  VmResult<VmLineNumberTable> result=VmResult.createFrom(object);
  if (object.has("result")) {
    result.setResult(VmLineNumberTable.createFrom(isolate,libraryId,url,object.getJSONObject("result")));
  }
  return result;
}
