{
  String[] program=new String[]{"module.exports = function (config) {","  config.set({","    ...","    plugins: [..., 'karma-coverage', ...],","    ...","  });","};"};
  return "<html><body>" + "Implementation of <code>'coverage'</code> reporter is not found.<br/>" + "<div style='padding-top:3px; padding-bottom:3px\' >"+ "Make sure <code>'karma-coverage'</code> plugin is specified."+ " </div>"+ "<pre><code>" + StringUtil.join(program,"\n") + "</code></pre>"+ "</body></html>";
}
