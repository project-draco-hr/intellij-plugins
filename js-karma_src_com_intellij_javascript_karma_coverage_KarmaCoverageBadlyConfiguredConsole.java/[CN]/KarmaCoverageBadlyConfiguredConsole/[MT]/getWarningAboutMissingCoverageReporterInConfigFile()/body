{
  String[] program=new String[]{"module.exports = function (config) {","  config.set({","    ...","    reporters: [..., 'coverage', ...],","    ...","  });","};"};
  return "<html><body>" + "Make sure <code>'coverage'</code> reporter is specified in config file:" + "<pre><code>" + StringUtil.join(program,"\n") + "</code></pre>"+ "</body></html>";
}
