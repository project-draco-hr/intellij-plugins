{
  if (index == 0) {
    return 0;
  }
  int pos=positions[IndexHistory.DOUBLE][index];
  int originalPos=in.position();
  in.seek(pos);
  double value=in.readDouble();
  in.seek(originalPos);
  return value;
}
