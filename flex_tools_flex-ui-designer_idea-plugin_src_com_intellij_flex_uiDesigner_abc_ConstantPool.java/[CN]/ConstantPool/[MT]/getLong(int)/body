{
  if (index == 0) {
    return 0;
  }
  int pos=positions[IndexHistory.UINT][index];
  int originalPos=in.position();
  in.seek(pos);
  long value=in.readU32();
  in.seek(originalPos);
  return value;
}
