{
  if (index == 0) {
    return 0;
  }
  int pos=positions[IndexHistory.INT][index];
  int originalPos=in.position();
  in.seek(pos);
  int value=in.readU32();
  in.seek(originalPos);
  return value;
}
