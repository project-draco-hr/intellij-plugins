{
  int pos=s.indexOf('.',fromIndex);
  if (pos == -1 && s.indexOf(":") >= fromIndex)   pos=s.indexOf(":",fromIndex);
  if (pos == -1 && s.indexOf("#") >= fromIndex)   pos=s.indexOf("#",fromIndex);
  return pos;
}
