{
  myAsyncExecutionQueue.offer(new Runnable(){
    public void run(){
      try {
        myConnection.doGc(finished);
      }
 catch (      IOException e) {
        finished.finished(null,e);
      }
    }
  }
);
}
