{
  final List<DartPathInfo> result=new ArrayList<DartPathInfo>();
  for (  List<DartPathInfo> list : FileBasedIndex.getInstance().getValues(DART_IMPORT_INDEX,virtualFile.getName(),GlobalSearchScope.fileScope(project,virtualFile))) {
    result.addAll(list);
  }
  return result;
}
