{
  final List<DartImportInfo> result=new ArrayList<DartImportInfo>();
  for (  List<DartImportInfo> list : FileBasedIndex.getInstance().getValues(DART_IMPORT_INDEX,virtualFile.getName(),GlobalSearchScope.fileScope(project,virtualFile))) {
    result.addAll(list);
  }
  return result;
}
