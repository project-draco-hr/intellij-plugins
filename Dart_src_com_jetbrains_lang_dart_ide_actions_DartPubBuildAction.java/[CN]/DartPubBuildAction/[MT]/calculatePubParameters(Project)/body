{
  final DartPubBuildDialog dialog=new DartPubBuildDialog(project);
  dialog.show();
  return dialog.getExitCode() == DialogWrapper.OK_EXIT_CODE ? new String[]{"build","--mode=" + dialog.getPubBuildMode()} : null;
}
