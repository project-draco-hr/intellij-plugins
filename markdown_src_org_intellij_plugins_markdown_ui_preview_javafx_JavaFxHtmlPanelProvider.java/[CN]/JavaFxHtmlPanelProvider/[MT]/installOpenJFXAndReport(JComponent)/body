{
  final int answer=Messages.showYesNoDialog(parentComponent,"Would you like to download and install OpenJFX?","OpenJFX Installation",null);
  if (answer == Messages.NO) {
    return false;
  }
  return JavaFXInstallator.INSTANCE.installOpenJFXAndReport(parentComponent);
}
