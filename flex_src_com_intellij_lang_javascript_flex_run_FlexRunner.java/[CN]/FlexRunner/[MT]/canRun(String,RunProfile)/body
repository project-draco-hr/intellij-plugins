{
  return DefaultRunExecutor.EXECUTOR_ID.equals(executorId) && (profile instanceof FlexIdeRunConfiguration || profile instanceof NewFlexUnitRunConfiguration || (profile instanceof FlexRunConfiguration && ((FlexRunConfiguration)profile).getRunnerParameters().getRunMode() != FlexRunnerParameters.RunMode.ConnectToRunningFlashPlayer));
}
