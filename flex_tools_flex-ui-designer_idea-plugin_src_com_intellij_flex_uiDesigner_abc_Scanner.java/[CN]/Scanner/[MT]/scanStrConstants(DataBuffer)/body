{
  int size=in.readU32();
  int[] positions=new int[size];
  for (int i=1; i < size; i++) {
    positions[i]=in.position();
    int length=in.readU32();
    in.skip(length);
  }
  return positions;
}
