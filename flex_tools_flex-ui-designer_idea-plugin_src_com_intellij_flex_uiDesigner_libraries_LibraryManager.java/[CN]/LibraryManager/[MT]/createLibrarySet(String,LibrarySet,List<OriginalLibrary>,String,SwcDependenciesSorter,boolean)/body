{
  try {
    return new LibrarySet(id,parent,ApplicationDomainCreationPolicy.ONE,swcDependenciesSorter.sort(libraries,id,flexSdkVersion,isFromSdk));
  }
 catch (  Throwable e) {
    throw new InitException(e,"error.sort.libraries");
  }
}
