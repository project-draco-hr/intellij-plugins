{
  final Class<?> aClass=new File(PathUtil.getJarPathForClass(CucumberJavaRunConfiguration.class)).isDirectory() ? CucumberJvmSMFormatter.class : CucumberJavaRunConfiguration.class;
  return PathUtil.getJarPathForClass(aClass).replace("cucumber-java.jar","SMFormatter.jar");
}
