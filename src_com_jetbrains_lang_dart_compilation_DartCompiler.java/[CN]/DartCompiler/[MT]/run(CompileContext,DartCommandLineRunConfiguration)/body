{
  final Module module=configuration.getConfigurationModule().getModule();
  if (ModuleType.get(module) != DartModuleType.getInstance()) {
    return true;
  }
  if (module == null) {
    context.addMessage(CompilerMessageCategory.ERROR,DartBundle.message("no.module.for.run.configuration",configuration.getName()),null,-1,-1);
    return false;
  }
  return compileModule(context,module,configuration);
}
