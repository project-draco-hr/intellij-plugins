{
  int fileId=f.getId();
  if (swf != null) {
    SourceFile sameFile=m_fileInfo.similarFileInSwf(swf,f);
    if (sameFile != null)     fileId=sameFile.getId();
 else     fileId=-1;
  }
  Location l=(fileId > -1) ? breakEnableRequest(fileId,line) : null;
  return l;
}
