{
  final SearchScope _selectedScope=filterScope.getSelectedScope();
  if (_selectedScope instanceof AllSearchScope) {
    return null;
  }
  return _selectedScope instanceof GlobalSearchScope ? (GlobalSearchScope)_selectedScope : GlobalSearchScope.allScope(getProject());
}
