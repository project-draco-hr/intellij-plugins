{
  for (  String folder : POSSIBLE_FOLDERS_IN_PHONEGAP_ROOT) {
    Collection<VirtualFile> files=FilenameIndex.getVirtualFilesByName(project,folder,GlobalSearchScope.projectScope(project));
    if (!files.isEmpty()) {
      return files;
    }
  }
  return ContainerUtil.emptyList();
}
