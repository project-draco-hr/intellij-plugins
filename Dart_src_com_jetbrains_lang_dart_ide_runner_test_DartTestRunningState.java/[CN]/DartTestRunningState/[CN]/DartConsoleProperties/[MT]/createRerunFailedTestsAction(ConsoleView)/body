{
  if (ActionManager.getInstance().getAction("RerunFailedTests") == null)   return null;
  DartTestRerunnerAction action=new DartTestRerunnerAction(consoleView);
  action.init(this);
  return action;
}
