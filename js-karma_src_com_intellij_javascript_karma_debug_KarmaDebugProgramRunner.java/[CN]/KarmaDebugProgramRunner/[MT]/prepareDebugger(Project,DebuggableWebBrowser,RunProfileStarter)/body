{
  return debuggableWebBrowser.getDebugEngine().prepareDebugger(project,debuggableWebBrowser.getWebBrowser()).then(new Function<Unit,RunProfileStarter>(){
    @Override public RunProfileStarter fun(    Unit aVoid){
      return starter;
    }
  }
);
}
