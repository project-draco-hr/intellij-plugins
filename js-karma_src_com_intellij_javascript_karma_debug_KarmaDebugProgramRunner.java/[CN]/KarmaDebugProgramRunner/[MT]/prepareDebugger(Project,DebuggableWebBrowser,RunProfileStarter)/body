{
  if (debuggableWebBrowser == null) {
    return Promise.resolve(starter);
  }
 else {
    return debuggableWebBrowser.getDebugEngine().prepareDebugger(project,debuggableWebBrowser.getWebBrowser()).then(new Function<Void,RunProfileStarter>(){
      @Override public RunProfileStarter fun(      Void aVoid){
        return starter;
      }
    }
);
  }
}
