{
  if (responseSink == null || code == null || message == null) {
    throw new IllegalArgumentException("Unexpected null argument: " + responseSink + " "+ this.code+ " "+ this.message);
  }
  this.responseSink=responseSink;
  this.code=code;
  this.message=message;
}
