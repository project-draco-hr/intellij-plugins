{
  StringBuffer sb=new StringBuffer(lines.size() * 70);
  for (Iterator<String> it=lines.iterator(); it.hasNext(); ) {
    sb.append(it.next());
    if (it.hasNext()) {
      sb.append(delimeter);
    }
  }
  return sb.toString();
}
