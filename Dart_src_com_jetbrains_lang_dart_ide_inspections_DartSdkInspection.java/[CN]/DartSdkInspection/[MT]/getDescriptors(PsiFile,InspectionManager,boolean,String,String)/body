{
  return new ProblemDescriptor[]{manager.createProblemDescriptor(file,message,new LocalQuickFix[]{new MyQuickFix(fixMessage)},HighlightInfo.convertSeverityToProblemHighlight(getDefaultLevel().getSeverity()),isOnTheFly,false)};
}
