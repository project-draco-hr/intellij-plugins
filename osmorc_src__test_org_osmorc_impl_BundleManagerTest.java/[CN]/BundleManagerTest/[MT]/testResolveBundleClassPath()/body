{
  myBundleManager.addManifestHolder(makeHolder("MyManifest.MF","Bundle-SymbolicName: foo.bar\n" + "Bundle-Version: 1.0.0\n" + "Bundle-ClassPath: /naff/trot/"+ FAKE_JAR_NAME,getModule()));
  Set<Object> objects=myBundleManager.resolveDependenciesOf(getModule());
  assertThat(objects,notNullValue());
  assertThat(objects.contains(myFakeJarLibrary),is(true));
  assertThat(objects.size(),is(1));
}
