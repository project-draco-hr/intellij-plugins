{
  return "<html><body>" + TITLE + SEE_KARMA_SERVER_TAB+ "<div style='padding-top:3px'>Make sure <code>'coverage'</code> reporter is specified like this:</div>"+ formatHtmlCode(new String[]{"module.exports = function (config) {","  config.set({","    ...","    reporters: [..., 'coverage'],","    ...","  });","};"})+ "As the reporter is specified, run with coverage again."+ "</body></html>";
}
