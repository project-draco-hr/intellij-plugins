{
  if (Library.getSwfFile(file) == null || Library.getCatalogFile(file) == null) {
    LogMessageUtil.LOG.warn("SWC is corrupted (library.swf or catalog.xml doesn't exists): " + file.getPath());
    return false;
  }
  return true;
}
