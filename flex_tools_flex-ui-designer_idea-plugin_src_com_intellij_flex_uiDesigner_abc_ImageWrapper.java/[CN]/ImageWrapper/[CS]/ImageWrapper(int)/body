{
  this.dataLength=dataLength;
  initAbcBlank();
  totalLength=SWF_HEADER_P1.length + 4 + SWF_HEADER_P2.length+ B_ABC.length+ 6+ 2+ dataLength+ 2+ SYMBOL_CLASS_TAG_LENGTH+ SWF_FOOTER.length;
  buffer=BUFFER.get();
}
