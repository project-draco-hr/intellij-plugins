{
  boolean hasError=true;
  try {
    beginMessage(ClientMethod.closeProject);
    writeId(project);
    hasError=false;
  }
  finally {
    registeredProjects.remove(project);
    if (hasError) {
      blockOut.rollback();
    }
 else {
      out.flush();
    }
  }
}
