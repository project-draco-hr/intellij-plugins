{
  if (out == null) {
    return;
  }
  out.close();
  out=null;
  blockOut=null;
  registeredLibraryCounter=0;
  sessionId++;
  registeredModules.clear();
  mxmlWriter.reset();
  DocumentFileManager.getInstance().reset(sessionId);
}
