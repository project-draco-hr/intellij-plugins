{
  if (hasError) {
    blockOut.rollback();
    if (callback != null) {
      callback.setRejected();
    }
  }
 else {
    try {
      out.flush();
    }
 catch (    IOException e) {
      LogMessageUtil.processInternalError(e);
    }
  }
}
