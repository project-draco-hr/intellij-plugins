{
  if (hasError) {
    try {
      blockOut.rollback();
    }
  finally {
      if (callback != null) {
        callback.setRejected();
      }
    }
  }
 else {
    try {
      out.flush();
    }
 catch (    IOException e) {
      LogMessageUtil.processInternalError(e);
    }
  }
}
