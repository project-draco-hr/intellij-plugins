{
  final List<String> rslUrls=getRslUrls(sdk,swcPath);
  for (  String url : rslUrls) {
    if (url.startsWith("http://") || new File(sdk.getHomePath() + "/frameworks/rsls/" + url).isFile()) {
      return true;
    }
  }
  return false;
}
