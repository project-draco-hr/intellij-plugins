{
  if (myDatagramSocket != null) {
    myDatagramSocket.close();
  }
  myIsRunning=false;
  try {
    while (!myDisposed) {
      Thread.sleep(100);
    }
  }
 catch (  InterruptedException ignored) {
  }
}
