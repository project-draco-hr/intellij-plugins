{
  UIUtil.invokeLater(new Runnable(){
    public void run(){
      TreePath path=myTree.getPathForUser(user);
      if (path != null) {
        myTreeUi.invalidatePath(path);
        myTree.revalidate();
        myTree.repaint();
      }
    }
  }
);
}
