{
  if (text == null) {
    return null;
  }
  if (isFirstTime) {
    if (text.indexOf(TEST_RUN_COMMAND) > 0) {
      isActive=true;
    }
  }
  if (isActive) {
    return accumulate(text,contentType);
  }
 else {
    return null;
  }
}
