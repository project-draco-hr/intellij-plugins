{
  FlexSdk flexSdk=new FlexSdk(sdkHome);
  if (!flexSdk.isValid()) {
    return null;
  }
  FlexSdkUtils.setupSdkPaths(LocalFileSystem.getInstance().findFileByPath(sdkHome),null,flexSdk.createRootsModificator());
  mySdks.add(0,flexSdk);
  fireChanged();
  return flexSdk;
}
