{
  mySdks.clear();
  myUsedSkds.clear();
  if (source != null) {
    for (    FlexSdk sdk : source.getSdks()) {
      mySdks.add(sdk.getCopy());
      myUsedSkds.put(USED_BY_OTHERS,sdk.getHomePath());
    }
  }
  fireChanged();
}
