{
  try {
    return TextUtil.removeQuoting(context.evaluate("(char *)[[(id)rb_inspect(" + myValue.getVar().getPointer() + ") description] UTF8String]").getReadableValue());
  }
 catch (  DBCannotEvaluateException e) {
    CidrDebuggerLog.LOG.debug(e);
  }
  return super.doComputeValue(context);
}
