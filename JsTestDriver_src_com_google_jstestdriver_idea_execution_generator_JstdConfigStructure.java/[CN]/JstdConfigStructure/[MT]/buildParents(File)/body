{
  List<File> parents=Lists.newArrayList();
  File f=dir;
  while (f != null) {
    parents.add(f);
    if (FILE_SYSTEM_ROOTS.contains(f)) {
      break;
    }
    f=f.getParentFile();
  }
  Collections.reverse(parents);
  return parents;
}
