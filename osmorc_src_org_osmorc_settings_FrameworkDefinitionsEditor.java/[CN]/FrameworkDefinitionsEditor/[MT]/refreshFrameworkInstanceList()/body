{
  List<FrameworkInstanceDefinition> instanceDefinitions=getApplicationSettingsWorkingCopy().getFrameworkInstanceDefinitions();
  frameworkInstances.setListData(ArrayUtil.toObjectArray(instanceDefinitions));
}
