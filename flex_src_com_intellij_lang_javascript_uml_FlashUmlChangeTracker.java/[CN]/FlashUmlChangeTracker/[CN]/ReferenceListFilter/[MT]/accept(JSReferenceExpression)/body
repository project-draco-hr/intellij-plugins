{
  final PsiElement parent=element.getParent();
  return parent instanceof JSReferenceList && parent.getNode().findChildByType(myExtends ? JSTokenTypes.EXTENDS_KEYWORD : JSTokenTypes.IMPLEMENTS_KEYWORD) != null;
}
