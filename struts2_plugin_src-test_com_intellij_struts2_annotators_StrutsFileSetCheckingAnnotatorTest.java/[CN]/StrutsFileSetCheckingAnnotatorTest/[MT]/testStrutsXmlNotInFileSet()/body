{
  createStrutsFileSet("struts-simple.xml");
  final IntentionAction intention=myFixture.getAvailableIntention("Add struts-default.xml to file set","struts-default.xml");
  assertIntentionFound(intention);
}
