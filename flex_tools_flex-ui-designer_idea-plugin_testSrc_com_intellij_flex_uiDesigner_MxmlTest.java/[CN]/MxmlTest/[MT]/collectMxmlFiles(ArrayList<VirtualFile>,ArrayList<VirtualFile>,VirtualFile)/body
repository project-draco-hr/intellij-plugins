{
  for (  VirtualFile file : parent.getChildren()) {
    final String name=file.getName();
    if (name.charAt(0) == '.') {
    }
 else     if (name.startsWith("Aux")) {
      auxFiles.add(file);
    }
 else     if (name.endsWith(JavaScriptSupportLoader.MXML_FILE_EXTENSION_DOT) && !name.startsWith("T.") && !name.startsWith("TestApp.")&& !name.startsWith("ProjectMxmlComponentAsParentWithDefaultProperty.")&& !name.startsWith("GenericMxmlSupport.")) {
      files.add(file);
    }
    if (file.isDirectory()) {
      collectMxmlFiles(files,auxFiles,file);
    }
  }
}
