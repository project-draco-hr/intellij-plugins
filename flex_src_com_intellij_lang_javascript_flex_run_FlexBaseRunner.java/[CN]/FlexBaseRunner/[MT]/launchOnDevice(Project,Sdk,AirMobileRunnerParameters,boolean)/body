{
  final String appId=params.getAirMobileRunMode() == AirMobileRunMode.ExistingPackage ? MobileAirUtil.getAppIdFromPackage(params.getExistingPackagePath()) : getSwfPathAndApplicationId(params).second;
  if (appId == null) {
    Messages.showErrorDialog(project,"Failed to get application id","Error");
  }
  return launchOnDevice(project,flexSdk,params,appId,isDebug);
}
