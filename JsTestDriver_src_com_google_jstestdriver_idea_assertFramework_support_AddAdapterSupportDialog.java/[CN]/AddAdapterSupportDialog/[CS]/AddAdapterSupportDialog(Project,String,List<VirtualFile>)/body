{
  super(project);
  myAssertFrameworkName=assertionFrameworkName;
  myAdapterSourceFiles=adapterSourceFiles;
  setModal(true);
  updateAssertionFrameworkSpecificDescriptions();
  myDirectoryTypeManager=ExtractDirectoryTypeManager.install(project,myDirectoryTypeContent,assertionFrameworkName,myDefaultRadioButton,myCustomRadioButton);
  myNewLibraryCreationManager=NewLibraryCreationManager.install(project,myNewLibraryNameTextField,assertionFrameworkName);
  myDirectoryTypeManager.addChangeListener(myNewLibraryCreationManager);
  myDirectoryTypeManager.init();
  super.init();
}
