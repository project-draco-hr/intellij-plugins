{
  List<State> states=nameToState.get(name);
  if (states == null) {
    states=new ArrayList<State>(5);
    states.add(state);
    nameToState.put(name,states);
  }
 else {
    states.add(state);
  }
}
