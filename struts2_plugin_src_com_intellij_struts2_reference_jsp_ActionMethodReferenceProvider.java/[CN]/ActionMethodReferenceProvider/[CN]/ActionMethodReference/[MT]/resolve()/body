{
  if (action == null) {
    return null;
  }
  return ContainerUtil.find(action.getActionMethods(),new Condition<PsiMethod>(){
    @Override public boolean value(    final PsiMethod psiMethod){
      return Comparing.equal(psiMethod.getName(),methodName);
    }
  }
);
}
