{
  ApplicationManager.getApplication().invokeLater(new Runnable(){
    public void run(){
      ToolWindowManager.getInstance(myModule.getProject()).notifyByBalloon(ToolWindowId.DEBUG,MessageType.INFO,FlexBundle.message("ios.application.installed.please.launch",myAppName));
    }
  }
);
}
