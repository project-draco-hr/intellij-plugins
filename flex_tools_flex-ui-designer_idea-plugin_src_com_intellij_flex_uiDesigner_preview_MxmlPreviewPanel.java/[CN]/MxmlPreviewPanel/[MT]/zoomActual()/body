{
  if (image == null) {
    return;
  }
  if (zoomToFit && imagePanel.getWidth() >= image.getWidth() && imagePanel.getHeight() >= image.getHeight()) {
    return;
  }
  zoomToFit=false;
  setZoomFactor(1.0);
}
