{
  return nature.isLib() ? LinkageType.External : nature.pureAS ? LinkageType.Merged : nature.isWebPlatform() ? StringUtil.compareVersionNumbers(sdkVersion,"4") >= 0 ? LinkageType.RSL : LinkageType.Merged : LinkageType.Merged;
}
