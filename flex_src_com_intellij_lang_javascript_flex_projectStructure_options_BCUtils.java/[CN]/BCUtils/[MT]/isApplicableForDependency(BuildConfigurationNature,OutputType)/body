{
  if (dependantNature.isLib()) {
    return dependencyOutputType == OutputType.Library;
  }
 else {
    return dependencyOutputType == OutputType.Library || dependencyOutputType == OutputType.RuntimeLoadedModule;
  }
}
