{
  if (dependantNature.isLib()) {
    return dependencyOutputType == FlexIdeBuildConfiguration.OutputType.Library;
  }
 else {
    return dependencyOutputType == FlexIdeBuildConfiguration.OutputType.Library || dependencyOutputType == FlexIdeBuildConfiguration.OutputType.RuntimeLoadedModule;
  }
}
