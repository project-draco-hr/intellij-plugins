{
  return ContainerUtil.map2Array(myTreeItems.get(module),BCNode.class,new Function<FlexIdeBCConfigurable,BCNode>(){
    @Override public BCNode fun(    FlexIdeBCConfigurable configurable){
      return new BCNode(configurable);
    }
  }
);
}
