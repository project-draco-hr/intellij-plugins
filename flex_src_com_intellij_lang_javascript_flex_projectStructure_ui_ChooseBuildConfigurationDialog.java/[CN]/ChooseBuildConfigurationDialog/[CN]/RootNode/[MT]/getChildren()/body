{
  return ContainerUtil.map2Array(myTreeItems.keySet(),SimpleNode.class,new Function<Module,SimpleNode>(){
    @Override public SimpleNode fun(    Module module){
      return new ModuleNode(module);
    }
  }
);
}
