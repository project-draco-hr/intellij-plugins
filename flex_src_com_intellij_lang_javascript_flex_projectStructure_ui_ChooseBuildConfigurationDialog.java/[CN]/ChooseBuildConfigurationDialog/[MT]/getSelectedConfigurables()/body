{
  return ContainerUtil.map2Array(mySelection,FlexIdeBCConfigurable.class,new Function<BCNode,FlexIdeBCConfigurable>(){
    @Override public FlexIdeBCConfigurable fun(    BCNode bcNode){
      return bcNode.configurable;
    }
  }
);
}
