{
  if (_binMarker != null) {
    if (errorMessage != null) {
      _binMarker.error(errorMessage);
    }
 else {
      _binMarker.done(_binMarkerType);
    }
    _binMarker=null;
    _binMarkerType=null;
  }
}
