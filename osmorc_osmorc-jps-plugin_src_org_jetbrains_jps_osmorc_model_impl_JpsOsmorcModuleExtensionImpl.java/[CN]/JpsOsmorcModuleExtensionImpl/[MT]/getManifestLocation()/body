{
  if (isUseProjectDefaultManifestFileLocation()) {
    JpsOsmorcProjectExtension projectExtension=getProjectExtension();
    return projectExtension == null ? OsmorcProjectExtensionProperties.DEFAULT_MANIFEST_LOCATION : projectExtension.getDefaultManifestFileLocation();
  }
 else {
    return doGetManifestLocation();
  }
}
