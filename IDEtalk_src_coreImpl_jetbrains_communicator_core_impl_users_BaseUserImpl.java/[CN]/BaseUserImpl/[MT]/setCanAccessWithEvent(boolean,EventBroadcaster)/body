{
  if (canAccessMyFiles == canAccessMyFiles())   return;
  broadcaster.doChange(new UserEvent.Updated(this,CAN_ACCESS_MY_FILES,canAccessMyFiles(),canAccessMyFiles),new Runnable(){
    @Override public void run(){
      myCanAccessMyFiles=canAccessMyFiles;
    }
  }
);
}
