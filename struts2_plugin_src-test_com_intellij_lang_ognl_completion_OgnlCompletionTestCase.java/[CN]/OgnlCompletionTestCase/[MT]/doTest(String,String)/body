{
  configureFromFileText("test." + OgnlFileType.INSTANCE.getDefaultExtension(),OgnlLanguage.EXPRESSION_PREFIX + ognlExpression + OgnlLanguage.EXPRESSION_SUFFIX);
  complete();
  Arrays.sort(expectedCompletionItems);
  assertStringItems(expectedCompletionItems);
}
