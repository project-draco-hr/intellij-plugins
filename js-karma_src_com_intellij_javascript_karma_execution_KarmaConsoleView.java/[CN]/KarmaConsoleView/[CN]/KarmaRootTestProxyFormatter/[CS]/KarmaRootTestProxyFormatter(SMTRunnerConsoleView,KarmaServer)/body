{
  myRenderer=ObjectUtils.tryCast(consoleView.getResultsViewer().getTreeView().getCellRenderer(),TestTreeRenderer.class);
  if (myRenderer != null) {
    myRenderer.setAdditionalRootFormatter(this);
  }
  myServer=server;
}
