{
  myTestTreeRenderer=ObjectUtils.tryCast(consoleView.getResultsViewer().getTreeView().getCellRenderer(),TestTreeRenderer.class);
  if (myTestTreeRenderer != null) {
    myTestTreeRenderer.setAdditionalRootFormatter(this);
  }
}
