{
  final CompilerOptions compilerOptions=myBCManager.getModuleLevelCompilerOptions();
  final boolean changed=ShowSettingsUtil.getInstance().editConfigurable(myProject,new CompilerOptionsConfigurable(Mode.Module,myModule,myProject,compilerOptions));
  if (changed) {
    updateTreeTable();
  }
}
