{
  for (  PsiElement childElement : getChildren()) {
    if (childElement instanceof HbPath || childElement instanceof HbData) {
      return ((HbPsiElement)childElement).getName();
    }
  }
  return null;
}
