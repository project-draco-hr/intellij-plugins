{
  if (value == null) {
    return null;
  }
  final XmlElement element=context.getReferenceXmlElement();
  if (element == null) {
    return null;
  }
  return PathReferenceManager.getInstance().getCustomPathReference(value,context.getModule(),element,getResultContributors());
}
