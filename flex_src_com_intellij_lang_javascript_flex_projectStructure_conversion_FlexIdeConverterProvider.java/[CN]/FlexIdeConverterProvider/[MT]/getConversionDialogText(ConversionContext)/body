{
  return "Project '" + context.getProjectFile().getName() + "' needs to be converted to be opened in "+ ApplicationNamesInfo.getInstance().getProductName()+ ". Converted project will remain compatible with IntelliJ IDEA.";
}
