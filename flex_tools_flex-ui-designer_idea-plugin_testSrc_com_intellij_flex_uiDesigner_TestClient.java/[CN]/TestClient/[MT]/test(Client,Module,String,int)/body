{
  client.blockOut.end();
  client.out.write(1);
  client.out.writeAmfUtf(filename,false);
  if (module != null) {
    client.writeId(module,client.out);
  }
 else {
    client.out.writeShort(-1);
  }
  client.out.write(c);
  client.flush();
}
