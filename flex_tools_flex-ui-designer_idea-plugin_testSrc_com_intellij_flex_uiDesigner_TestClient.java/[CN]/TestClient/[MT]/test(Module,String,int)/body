{
  blockOut.end();
  out.write(CLASS);
  out.write(classId);
  out.write(0);
  if (module != null) {
    writeId(module,out);
  }
 else {
    out.writeShort(-1);
  }
  out.writeAmfUtf(filename,false);
  flush();
}
