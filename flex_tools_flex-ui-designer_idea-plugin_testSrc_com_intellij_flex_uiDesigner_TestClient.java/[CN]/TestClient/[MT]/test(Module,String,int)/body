{
  blockOut.end();
  out.write(CLASS);
  out.write(0);
  out.write(classId);
  if (module != null) {
    writeId(module,out);
  }
 else {
    out.writeShort(-1);
  }
  out.writeAmfUtf(filename,false);
  flush();
}
