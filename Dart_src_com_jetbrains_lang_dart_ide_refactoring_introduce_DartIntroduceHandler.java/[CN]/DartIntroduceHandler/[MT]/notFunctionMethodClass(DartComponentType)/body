{
  final boolean isFunctionMethodClass=type == DartComponentType.METHOD || type == DartComponentType.FUNCTION || type == DartComponentType.INTERFACE || type == DartComponentType.CLASS;
  return !isFunctionMethodClass;
}
