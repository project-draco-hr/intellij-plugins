{
  if (obj instanceof AnalysisErrorFixes) {
    AnalysisErrorFixes other=(AnalysisErrorFixes)obj;
    return ObjectUtilities.equals(other.error,error) && ObjectUtilities.equals(other.fixes,fixes);
  }
  return false;
}
