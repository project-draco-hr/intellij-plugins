{
  if (isConnected()) {
    return StringUtil.getMsg("JabberConnectionCommand.connected",myJabberFacade.getMyAccount().getJabberId() + '/' + JabberFacade.IDETALK_RESOURCE);
  }
 else {
    return StringUtil.getMsg("JabberConnectionCommand.disconnected");
  }
}
