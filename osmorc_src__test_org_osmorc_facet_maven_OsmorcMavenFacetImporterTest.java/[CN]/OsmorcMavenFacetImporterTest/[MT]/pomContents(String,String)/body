{
  return "<groupId>org.osmorc</groupId>" + "<artifactId>" + artifactId + "</artifactId>"+ "<version>1</version>"+ "<packaging>bundle</packaging>"+ "<build>"+ "  <plugins>"+ "    <plugin>"+ pluginCoordinates()+ instructions+ "    </plugin>"+ "  </plugins>"+ "</build>";
}
