{
  setFields();
  JPanel panel=FormBuilder.createFormBuilder().addLabeledComponent("PhoneGap/Cordova:",myExecutablePathField).getPanel();
  panel.setPreferredSize(new Dimension(600,40));
  return panel;
}
