{
  setFields();
  JPanel panel=FormBuilder.createFormBuilder().addLabeledComponent("Phonegap/Cordova executable:",myExecutablePathField).getPanel();
  panel.setPreferredSize(new Dimension(600,40));
  return panel;
}
