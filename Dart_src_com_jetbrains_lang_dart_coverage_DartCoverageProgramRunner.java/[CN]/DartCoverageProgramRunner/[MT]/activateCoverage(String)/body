{
  try {
    ProcessOutput output=new CapturingProcessHandler(new GeneralCommandLine().withExePath(dartPubPath).withParameters("global","activate","coverage")).runProcess(60 * 1000);
    return output.getExitCode() == 0;
  }
 catch (  ExecutionException e) {
    return false;
  }
}
