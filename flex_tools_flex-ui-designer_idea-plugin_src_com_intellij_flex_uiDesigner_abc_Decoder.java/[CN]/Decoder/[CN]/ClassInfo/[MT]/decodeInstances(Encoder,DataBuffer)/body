{
  for (int i=0, n=size(); i < n; i++) {
    int originalPos=in.position();
    in.seek(positions[i]);
    visitor.startInstance(in);
    decodeTraits(visitor,in);
    visitor.endInstance();
    in.seek(originalPos);
  }
}
