{
  return ContainerUtil.map2Array(columns,ColumnInfo.class,new Function<ColumnInfo,ColumnInfo>(){
    @Override public ColumnInfo fun(    ColumnInfo columnInfo){
      return new ColumnInfoWrapper(columnInfo);
    }
  }
);
}
