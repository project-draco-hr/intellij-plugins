{
  return executorId.equals(DefaultProfilerExecutor.EXECUTOR_ID) && (runProfile instanceof FlexRunConfiguration && !(runProfile instanceof AirRunConfiguration) || runProfile instanceof FlexIdeRunConfiguration);
}
