{
  final Collection<JSVariable> result=new ArrayList<JSVariable>();
  AngularJSProcessor.process(element,new Consumer<JSVariable>(){
    @Override public void consume(    JSVariable element){
      if (name.equals(element.getName())) {
        result.add(element);
      }
    }
  }
);
  return result;
}
