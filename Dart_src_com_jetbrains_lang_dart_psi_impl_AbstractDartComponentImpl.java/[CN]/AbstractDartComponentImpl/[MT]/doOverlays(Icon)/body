{
  if (isStatic()) {
    icon=overlayIcons(icon,AllIcons.Nodes.StaticMark);
  }
  if (isFinal()) {
    icon=overlayIcons(icon,AllIcons.Nodes.FinalMark);
  }
  if (isConst()) {
    icon=overlayIcons(icon,AllIcons.Nodes.FinalMark);
  }
  return icon;
}
