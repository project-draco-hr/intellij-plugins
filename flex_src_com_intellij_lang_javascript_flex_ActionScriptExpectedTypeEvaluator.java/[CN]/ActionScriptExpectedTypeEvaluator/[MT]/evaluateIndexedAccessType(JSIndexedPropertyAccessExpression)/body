{
  if (isASDictionaryAccess(node)) {
    myType=JSCommonTypeNames.OBJECT_CLASS_NAME;
  }
 else {
    final JSTypeSource typeSource=JSTypeSourceFactory.createTypeSource(myScope,true);
    myResult=new JSCompositeTypeImpl(typeSource,JSNamedType.createType(JSCommonTypeNames.INT_TYPE_NAME,typeSource,JSNamedType.StaticOrInstance.INSTANCE),JSNamedType.createType(JSCommonTypeNames.UINT_TYPE_NAME,typeSource,JSNamedType.StaticOrInstance.INSTANCE));
  }
}
