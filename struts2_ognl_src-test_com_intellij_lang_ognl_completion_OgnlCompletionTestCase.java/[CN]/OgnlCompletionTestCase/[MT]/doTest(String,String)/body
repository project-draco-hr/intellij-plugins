{
  configureFromFileText(OgnlTestUtils.DUMMY_OGNL_FILE_NAME,OgnlTestUtils.createExpression(ognlExpression));
  complete();
  Arrays.sort(expectedCompletionItems);
  assertStringItems(expectedCompletionItems);
}
