{
  if (FlexCompilerProjectConfiguration.getInstance(facet.getModule().getProject()).GENERATE_FLEXMOJOS_CONFIGS) {
    postTasks.add(new Flexmojos3GenerateConfigTask(project,mavenTree,FlexBuildConfiguration.getInstance(facet).CUSTOM_CONFIG_FILE,this));
  }
}
