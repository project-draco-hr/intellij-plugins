{
  for (  Group group : myGroupData.values()) {
    try {
      processGroupDone(group);
    }
 catch (    ParseException ex) {
    }
  }
  myTestData.clear();
  myGroupData.clear();
  mySuiteData.clear();
}
