{
  if (hasValidParent()) {
    try {
      return myName.substring(getParent().getName().length() + 1);
    }
 catch (    StringIndexOutOfBoundsException ex) {
      return myName;
    }
  }
 else {
    return myName;
  }
}
