{
  final String libraryName=DartIndexUtil.indexFile(inputData).getLibraryName();
  return libraryName == null ? Collections.<String,String>emptyMap() : Collections.singletonMap(inputData.getFile().getName(),libraryName);
}
