{
  myProject=project;
  myFilter=filter;
  myItems=new ArrayList<>();
  setColumnInfos(new ColumnInfo[]{createDescriptionColumn(),createLocationColumn()});
  setItems(myItems);
  setSortable(true);
}
