{
  if (inclusion) {
    if (super.accept(name)) {
      return true;
    }
  }
 else   if (!super.accept(name)) {
    return false;
  }
  for (  String s : startsWith) {
    if (StringUtil.startsWith(name,s)) {
      return inclusion;
    }
  }
  return !inclusion;
}
