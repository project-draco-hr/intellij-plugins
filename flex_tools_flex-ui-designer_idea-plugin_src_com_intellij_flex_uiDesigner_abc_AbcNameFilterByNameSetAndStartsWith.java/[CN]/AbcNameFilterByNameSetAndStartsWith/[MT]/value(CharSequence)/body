{
  if (inclusion) {
    if (super.value(name)) {
      return true;
    }
  }
 else   if (!super.value(name)) {
    return false;
  }
  for (  String s : startsWith) {
    if (StringUtil.startsWith(name,s)) {
      return inclusion;
    }
  }
  return !inclusion;
}
