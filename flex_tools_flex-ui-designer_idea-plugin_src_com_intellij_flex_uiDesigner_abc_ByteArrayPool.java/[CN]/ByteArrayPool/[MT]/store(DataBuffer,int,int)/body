{
  ByteArray a=wrappers.isEmpty() ? null : wrappers.pop();
  if (a == null) {
    a=newByteArray();
  }
  a.clear();
  a.data=data;
  a.start=start;
  a.end=end;
  a.init();
  int index=map.size() + 1;
  map.put(a,index);
  return index;
}
