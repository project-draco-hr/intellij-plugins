{
  final CollectConsumer<Pair<Module,FlexIdeBuildConfiguration>> consumer=new CollectConsumer<Pair<Module,FlexIdeBuildConfiguration>>();
  iterateRecursively((CheckedTreeNode)getModel().getRoot(),consumer);
  return consumer.getResult();
}
