{
  if ((count - lastBlockBegin) == SERVICE_DATA_SIZE) {
    return;
  }
  writeHeader();
  if (count >= 8192) {
    flushBuffer();
  }
 else {
    lastBlockBegin=count;
    count+=SERVICE_DATA_SIZE;
  }
}
