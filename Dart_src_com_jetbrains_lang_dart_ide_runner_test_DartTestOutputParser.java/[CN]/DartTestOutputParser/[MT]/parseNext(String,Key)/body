{
  if (text == null) {
    return false;
  }
  if (text.indexOf(TEST_RUN_COMMAND) > 0) {
    isActive=true;
  }
  if (isActive) {
    return accumulate(text,contentType);
  }
 else {
    return false;
  }
}
