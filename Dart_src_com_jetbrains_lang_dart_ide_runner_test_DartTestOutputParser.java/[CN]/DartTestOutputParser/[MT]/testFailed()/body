{
  if (myCurrentTestName.isEmpty())   return;
  testStarted();
  splitFailureMessage();
  myProcessor.fireOnTestFailure(new TestFailedEvent(myCurrentTestName,myTestId,myFailureMessage,myStackTrace,myTestError,myFailureActualText,myFailureExpectedText,null,0L));
  resetTestState();
}
