{
  if (myCurrentTestName.isEmpty())   return;
  testStarted();
  splitFailureMessage();
  myProcessor.signalTestFailure(myCurrentTestName,myTestId,myFailureMessage,myStackTrace,myTestError,myFailureActualText,myFailureExpectedText,null,0L);
  resetTestState();
}
