{
  if (isConnected()) {
    return StringUtil.getMsg("EditJabberSettingsCommand.connected",myJabberFacade.getMyAccount().getJabberId() + '/' + JabberFacade.IDETALK_RESOURCE);
  }
 else {
    return StringUtil.getMsg("EditJabberSettingsCommand.disconnected");
  }
}
