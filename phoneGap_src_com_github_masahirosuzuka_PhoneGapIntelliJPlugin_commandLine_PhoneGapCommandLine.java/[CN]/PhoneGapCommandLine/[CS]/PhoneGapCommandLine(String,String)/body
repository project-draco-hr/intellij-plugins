{
  myWorkDir=dir;
  myPath=path;
  try {
    version=executeAndReturnResult(myPath,"--version").replace("\"","").trim();
  }
 catch (  Exception e) {
    version=null;
    LOGGER.debug(e.getMessage(),e);
    myIsCorrect=false;
  }
}
