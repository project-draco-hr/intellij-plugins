{
  for (  String testName : testCase.getTests()) {
    TestResultProtocolMessage message=TestResultProtocolMessage.fromDryRun(myJstdConfigFile,browser,testCase.getName(),testName);
    writeTestResultProtocolMessage(message);
  }
}
