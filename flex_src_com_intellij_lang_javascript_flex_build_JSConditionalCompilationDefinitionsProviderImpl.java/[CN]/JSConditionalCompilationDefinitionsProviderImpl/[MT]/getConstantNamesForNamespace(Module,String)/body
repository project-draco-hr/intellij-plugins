{
  final Collection<String> result=new ArrayList<String>();
  if (module != null && !StringUtil.isEmpty(namespace)) {
    final String beginning=namespace + "::";
    iterateConditionalCompilationDefinitions(module,new Function<ConditionalCompilationDefinition,Boolean>(){
      public Boolean fun(      final ConditionalCompilationDefinition definition){
        if (definition.NAME.startsWith(beginning)) {
          result.add(definition.NAME.substring(beginning.length()));
        }
        return true;
      }
    }
);
  }
  return result;
}
