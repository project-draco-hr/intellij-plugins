{
  if (obj instanceof InlineMethodOptions) {
    InlineMethodOptions other=(InlineMethodOptions)obj;
    return other.deleteSource == deleteSource && other.inlineAll == inlineAll;
  }
  return false;
}
