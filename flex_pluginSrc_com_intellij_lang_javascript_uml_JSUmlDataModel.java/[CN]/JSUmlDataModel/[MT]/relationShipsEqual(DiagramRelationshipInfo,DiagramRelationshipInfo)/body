{
  if (r1 instanceof FlashDiagramRelationship) {
    return r2 instanceof FlashDiagramRelationship && ((FlashDiagramRelationship)r1).getType().equals(((FlashDiagramRelationship)r2).getType());
  }
  return r1 == r2;
}
