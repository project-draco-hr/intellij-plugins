{
  final Map<String,List<Action>> packageToActionMap=CachedValuesManager.getManager(myMergedModel.getManager().getProject()).getCachedValue(myMergedModel,actionsCachedValueProvider);
  return packageToActionMap.get(StringUtil.notNullize(namespace,EMPTY_NAMESPACE));
}
