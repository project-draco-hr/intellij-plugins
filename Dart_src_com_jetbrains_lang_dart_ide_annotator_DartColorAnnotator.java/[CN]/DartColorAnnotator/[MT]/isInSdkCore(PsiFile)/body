{
  final VirtualFile virtualFile=psiFile.getVirtualFile();
  return virtualFile != null && virtualFile.getParent() == LocalFileSystem.getInstance().findFileByPath(DartSettings.getSettings().getSdkPath() + "/lib/core");
}
