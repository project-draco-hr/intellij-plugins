{
  if (file.getFileType() == FileTypes.ARCHIVE && ("swc".equalsIgnoreCase(file.getExtension()) || "ane".equalsIgnoreCase(file.getExtension()))) {
    final VirtualFile local=file.getFileSystem() instanceof JarFileSystem ? file : JarFileSystem.getInstance().getJarRootForLocalFile(file);
    if (local == null)     return;
    final VirtualFile catalog=local.findChild("catalog.xml");
    if (catalog == null)     return;
    processCatalogFile(module,contextsOfModule,catalog);
  }
}
