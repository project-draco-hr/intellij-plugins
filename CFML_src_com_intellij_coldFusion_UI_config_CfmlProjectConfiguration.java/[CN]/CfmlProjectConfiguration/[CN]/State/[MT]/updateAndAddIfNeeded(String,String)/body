{
  if (logicalPath == null || directory == null) {
    return;
  }
  myMapps.putToServerMappings(StringUtil.startsWithChar(directory,'/') || StringUtil.startsWithChar(directory,'\\') ? directory : "/" + directory,logicalPath);
}
