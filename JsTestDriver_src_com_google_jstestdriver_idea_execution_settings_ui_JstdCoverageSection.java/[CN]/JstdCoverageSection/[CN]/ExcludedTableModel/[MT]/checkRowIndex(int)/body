{
  if (rowIndex < 0 || rowIndex >= myExcludedFiles.size()) {
    throw new IllegalStateException("Requested excluded file is out of bound: (rowIndex: " + rowIndex + ", size: "+ myExcludedFiles.size()+ ")");
  }
}
