{
  final IElementType elementType=element.getNode().getElementType();
  if (OgnlTokenTypes.TEXT.contains(elementType) || elementType == OgnlTokenTypes.IDENTIFIER) {
    return TEXT_TOKENIZER;
  }
  return EMPTY_TOKENIZER;
}
