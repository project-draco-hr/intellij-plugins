{
  String lineS=String.valueOf(line);
  int padding=6 - lineS.length();
  if (markCurrent && isCurrentLocation(module,line))   sb.append('=');
 else   sb.append(' ');
  sb.append(lineS);
  repeat(sb,' ',padding);
  sb.append(s);
}
