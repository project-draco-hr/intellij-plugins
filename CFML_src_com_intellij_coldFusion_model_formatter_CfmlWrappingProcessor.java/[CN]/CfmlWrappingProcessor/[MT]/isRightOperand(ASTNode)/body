{
  final ASTNode secondExpression=myNode.findChildByType(CfmlElementTypes.EXPRESSIONS,myNode.findChildByType(CfmlFormatterUtil.BINARY_OPERATORS));
  if (child == secondExpression)   return true;
  return false;
}
