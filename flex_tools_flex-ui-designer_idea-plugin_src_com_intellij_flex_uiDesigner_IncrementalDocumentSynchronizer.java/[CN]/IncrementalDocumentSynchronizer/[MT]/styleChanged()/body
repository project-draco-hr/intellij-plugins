{
  DesignerApplicationManager.getInstance().renderDocumentsAndCheckLocalStyleModification(new Document[]{FileDocumentManager.getInstance().getCachedDocument(event.getFile().getViewProvider().getVirtualFile())},true);
}
