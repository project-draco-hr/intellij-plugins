{
  return LookupElementBuilder.create(StringUtil.notNullize(target.getName().getStringValue())).withIcon(ElementPresentationManager.getIcon(target)).withTypeText(DomUtil.getFile(target).getName());
}
