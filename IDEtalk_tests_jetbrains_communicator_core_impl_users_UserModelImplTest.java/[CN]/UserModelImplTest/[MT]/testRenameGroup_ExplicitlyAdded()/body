{
  myUserModel.addGroup("new group");
  myUserModel.renameGroup("new group","new group1");
  assertEquals("Wrong groups after rename",Arrays.asList(new Object[]{"group1","group2","new group1"}),Arrays.asList(myUserModel.getGroups()));
}
