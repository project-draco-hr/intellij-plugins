{
  String typeText="";
  final DartReturnType returnType=PsiTreeUtil.getChildOfType(namedComponent,DartReturnType.class);
  if (returnType != null) {
    typeText=DartPresentableUtil.buildTypeText(namedComponent,returnType.getType(),resolveResult.getSpecialization());
  }
  return new DartFunctionDescription(namedComponent.getName(),typeText,DartParameterDescription.getParameters(namedComponent,resolveResult.getSpecialization()),DartNamedParameterDescription.getParameters(namedComponent,resolveResult.getSpecialization()));
}
