{
  return ContainerUtil.map2Array(StrutsVersion.values(),String.class,new Function<StrutsVersion,String>(){
    public String fun(    final StrutsVersion strutsVersion){
      return strutsVersion.toString();
    }
  }
);
}
