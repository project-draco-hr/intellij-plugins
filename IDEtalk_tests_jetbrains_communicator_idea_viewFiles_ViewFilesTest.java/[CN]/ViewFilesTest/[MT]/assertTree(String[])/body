{
  assertEquals("wrong number of nodes",nodes.length,getTree().getRowCount());
  for (int i=0; i < nodes.length; i++) {
    assertEquals("wrong node " + i,nodes[i],getTree().getPathForRow(i).getLastPathComponent().toString());
  }
}
