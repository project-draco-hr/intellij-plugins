{
  ProjectsData projectsData=new ProjectsData();
  projectsData.setProjectFiles("ideTalk",new VFile[]{VFile.create("build.xml")});
  myViewFilesPanel.refreshData(myUser,projectsData);
  assertTree(new String[]{"ideTalk","build.xml"});
}
