{
  if (list.contains(jarFile)) {
    return list.getInfo(jarFile);
  }
 else {
    final String path=virtualFile.getPath();
    OriginalLibrary library=new OriginalLibrary(virtualFile.getNameWithoutExtension() + "." + Integer.toHexString(path.hashCode()),jarFile,fromSdk);
    initializer.consume(library);
    return library;
  }
}
