{
  if ((parentToTraverse instanceof MarkdownListItemImpl || parentToTraverse instanceof MarkdownBlockQuoteImpl) && ((MarkdownCompositePsiElementBase)parentToTraverse).hasTrivialChildren()) {
    return true;
  }
  return false;
}
