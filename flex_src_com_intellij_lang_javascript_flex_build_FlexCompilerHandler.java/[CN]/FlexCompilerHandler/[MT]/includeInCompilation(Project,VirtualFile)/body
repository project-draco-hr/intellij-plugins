{
  if (CompilerManager.getInstance(project).isExcludedFromCompilation(file)) {
    return false;
  }
  return file.getFileType() == ActionScriptFileType.INSTANCE || JavaScriptSupportLoader.isMxmlOrFxgFile(file);
}
