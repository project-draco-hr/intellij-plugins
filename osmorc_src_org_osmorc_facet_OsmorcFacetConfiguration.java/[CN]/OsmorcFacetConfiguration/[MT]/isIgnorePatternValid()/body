{
  if (_ignoreFilePattern == null || _ignoreFilePattern.equals("")) {
    return true;
  }
  try {
    Pattern p=Pattern.compile(_ignoreFilePattern);
    return true;
  }
 catch (  Exception e) {
    return false;
  }
}
