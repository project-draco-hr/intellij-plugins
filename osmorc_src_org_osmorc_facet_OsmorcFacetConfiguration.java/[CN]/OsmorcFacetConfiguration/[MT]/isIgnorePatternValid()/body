{
  if (myIgnoreFilePattern == null || myIgnoreFilePattern.length() == 0) {
    return true;
  }
  try {
    Pattern.compile(myIgnoreFilePattern);
    return true;
  }
 catch (  Exception e) {
    return false;
  }
}
