{
  Map<String,PhoneGapRepoPackage> value=CACHED_REPO;
  if (value == null) {
    lock.lock();
    value=CACHED_REPO;
    if (value == null) {
      value=listNoCache();
      CACHED_REPO=value;
    }
  }
  return value;
}
