{
  try {
    return ".*".equals(getRuleRegex()) || (!"".equals(getRuleRegex()) && Pattern.compile(getRuleRegex()).matcher(libraryName).matches());
  }
 catch (  PatternSyntaxException e) {
    return false;
  }
}
