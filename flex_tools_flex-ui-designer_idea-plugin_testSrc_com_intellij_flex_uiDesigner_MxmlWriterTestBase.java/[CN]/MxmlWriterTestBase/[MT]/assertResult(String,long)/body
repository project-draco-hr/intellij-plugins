{
  boolean result=reader.readBoolean();
  if (result) {
    System.out.print(" passed (" + time + ")\n");
    passedCounter++;
    LOG.info(documentName + " passed");
  }
 else {
    fail(documentName + "\n" + reader.readUTF());
  }
}
