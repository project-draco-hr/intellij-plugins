{
  if (e instanceof InvalidPropertyException) {
    add(((InvalidPropertyException)e));
  }
 else {
    LOG.error(LogMessageEx.createEvent(e.getMessage(),ExceptionUtil.getThrowableText(e),LogMessageUtil.createMxmlTitle(currentFile),null,LogMessageUtil.createAttachment(currentFile)));
  }
}
