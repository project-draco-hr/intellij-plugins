{
  if (e instanceof InvalidPropertyException) {
    add(((InvalidPropertyException)e));
  }
 else {
    LogMessageEx.createEvent(e.getMessage(),ExceptionUtil.getThrowableText(e),LogMessageUtil.createTitle(currentFile),null,LogMessageUtil.createAttachment(currentFile));
  }
}
