{
  final String filePath=getFilePath();
  if (StringUtil.isEmpty(filePath)) {
    throw new ExecutionException("Empty file path");
  }
  return new DartCommandLineRunningState(env,getModule(),filePath,StringUtil.notNullize(getVMOptions()),StringUtil.notNullize(getArguments()));
}
