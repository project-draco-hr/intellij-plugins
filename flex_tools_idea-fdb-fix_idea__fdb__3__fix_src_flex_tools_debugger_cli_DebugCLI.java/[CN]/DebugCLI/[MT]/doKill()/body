{
  if (m_session == null)   err(getLocalizationManager().getLocalizedTextString("programNotBeingRun"));
 else {
    if (yesNoQuery(getLocalizationManager().getLocalizedTextString("askKillProgram")))     exitSession();
  }
}
