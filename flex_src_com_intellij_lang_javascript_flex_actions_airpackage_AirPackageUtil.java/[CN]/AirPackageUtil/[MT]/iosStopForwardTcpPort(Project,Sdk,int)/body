{
  final String idbPath=sdk.getHomePath() + IDB_RELATIVE_PATH;
  try {
    Runtime.getRuntime().exec(new String[]{idbPath,"-stopforward",String.valueOf(usbDebugPort)});
  }
 catch (  IOException e) {
    Logger.getInstance(AirPackageUtil.class.getName()).warn(e);
  }
}
