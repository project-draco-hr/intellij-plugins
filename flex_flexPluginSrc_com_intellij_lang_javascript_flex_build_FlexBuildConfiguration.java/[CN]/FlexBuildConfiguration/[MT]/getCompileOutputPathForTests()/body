{
  return isThisFlexFacetConfiguration && USE_FACET_COMPILE_OUTPUT_PATH ? FACET_COMPILE_OUTPUT_PATH_FOR_TESTS : VfsUtil.urlToPath(CompilerModuleExtension.getInstance(myModule).getCompilerOutputUrlForTests());
}
