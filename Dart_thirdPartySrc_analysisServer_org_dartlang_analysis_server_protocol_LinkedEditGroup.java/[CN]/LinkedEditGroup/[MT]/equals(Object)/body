{
  if (obj instanceof LinkedEditGroup) {
    LinkedEditGroup other=(LinkedEditGroup)obj;
    return ObjectUtilities.equals(other.positions,positions) && other.length == length && ObjectUtilities.equals(other.suggestions,suggestions);
  }
  return false;
}
