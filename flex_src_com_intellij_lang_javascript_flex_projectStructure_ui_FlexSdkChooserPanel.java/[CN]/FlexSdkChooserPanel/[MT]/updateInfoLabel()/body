{
  if (mySdk == null) {
    if (StringUtil.isEmpty(getSdkPath())) {
      myInfoLabel.setText("");
    }
 else {
      myInfoLabel.setText("SDK not found");
    }
  }
 else {
    myInfoLabel.setText("");
  }
}
