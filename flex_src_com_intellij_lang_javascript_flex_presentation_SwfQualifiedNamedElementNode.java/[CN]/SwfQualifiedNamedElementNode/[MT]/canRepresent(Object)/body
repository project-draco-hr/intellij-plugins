{
  JSQualifiedNamedElement value=getValue();
  return value != null && element != null && value.getClass() == element.getClass() && Comparing.equal(value.getQualifiedName(),((JSQualifiedNamedElement)element).getQualifiedName());
}
