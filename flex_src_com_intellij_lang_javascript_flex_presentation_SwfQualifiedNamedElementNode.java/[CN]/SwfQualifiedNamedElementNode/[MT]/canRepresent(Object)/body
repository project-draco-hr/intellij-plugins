{
  JSQualifiedNamedElement value=getValue();
  return value != null && value.isValid() && element != null && value.getClass() == element.getClass() && Comparing.equal(value.getQualifiedName(),((JSQualifiedNamedElement)element).getQualifiedName());
}
