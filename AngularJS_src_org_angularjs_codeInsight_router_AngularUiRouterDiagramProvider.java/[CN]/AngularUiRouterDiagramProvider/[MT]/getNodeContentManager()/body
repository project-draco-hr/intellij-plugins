{
  return new DiagramNodeContentManager(){
    @Override public boolean isInCategory(    Object element,    DiagramCategory category,    DiagramState presentation){
      if (element instanceof DiagramObject) {
        return ((DiagramObject)element).getType().getCategory().equals(category);
      }
      return false;
    }
    @Override public DiagramCategory[] getContentCategories(){
      return CATEGORIES;
    }
  }
;
}
