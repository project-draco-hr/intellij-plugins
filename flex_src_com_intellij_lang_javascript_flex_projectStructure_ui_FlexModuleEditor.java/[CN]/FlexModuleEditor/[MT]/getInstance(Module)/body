{
  ModuleEditor e=ModuleStructureConfigurable.getInstance(module.getProject()).getContext().getModulesConfigurator().getModuleEditor(module);
  e.getPanel();
  return (FlexModuleEditor)e.getEditor(DISPLAY_NAME);
}
