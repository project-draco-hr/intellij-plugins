{
  final VirtualFile file=containedUri.getScheme() == null ? VfsUtil.findRelativeFile(myFile.getParent(),containedUri.toString()) : LocalFileSystem.getInstance().findFileByPath(containedUri.getPath());
  return file == null ? null : getSource(myProject,file);
}
