{
  final VirtualFile file=relativeUri.getScheme() == null ? VfsUtilCore.findRelativeFile(relativeUri.toString(),myFile.getParent()) : LocalFileSystem.getInstance().findFileByPath(relativeUri.getPath());
  return file == null ? null : getSource(myProject,file).getUri();
}
