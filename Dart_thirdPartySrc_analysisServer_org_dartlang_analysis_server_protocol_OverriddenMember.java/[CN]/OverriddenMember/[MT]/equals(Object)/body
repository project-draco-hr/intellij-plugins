{
  if (obj instanceof OverriddenMember) {
    OverriddenMember other=(OverriddenMember)obj;
    return ObjectUtilities.equals(other.element,element) && ObjectUtilities.equals(other.className,className);
  }
  return false;
}
