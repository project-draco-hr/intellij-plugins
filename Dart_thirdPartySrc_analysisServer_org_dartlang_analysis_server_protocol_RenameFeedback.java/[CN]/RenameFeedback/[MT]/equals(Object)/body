{
  if (obj instanceof RenameFeedback) {
    RenameFeedback other=(RenameFeedback)obj;
    return other.offset == offset && other.length == length && ObjectUtilities.equals(other.elementKindName,elementKindName) && ObjectUtilities.equals(other.oldName,oldName);
  }
  return false;
}
