{
  final int off=4 + transientNameString.length() + 1;
  buffer.position(buffer.position() + off);
  decoders.add(new Decoder(new BufferWrapper(buffer,length - off),transientNameString.charAt(0) == '_' ? transientNameString.cloneChars() : null));
}
