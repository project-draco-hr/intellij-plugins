{
  if (element.getParent() instanceof DartImportOrExportStatement) {
    return ContainerUtil.map2Array(DartLibraryIndex.getAllStandardLibrariesFromSdk(element.getProject()),new Function<String,Object>(){
      @Override public Object fun(      final String sdkLib){
        return DartUrlResolver.DART_PREFIX + sdkLib;
      }
    }
);
  }
  return EMPTY_ARRAY;
}
