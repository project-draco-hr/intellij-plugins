{
  PsiBuilder builder=new PsiBuilderImpl(lexer,TokenSet.EMPTY,TokenSet.EMPTY,"Import-Package: com.acme;=\"1.0.0\"\n");
  ASTNode node=testObject.parse(ManifestParserDefinition.FILE,builder);
  assertThat(node.getElementType(),sameInstance((IElementType)ManifestParserDefinition.FILE));
  checkContainsNodes(node,ManifestElementTypes.SECTION);
  node=node.getChildren(null)[0];
  checkContainsNodes(node,ManifestElementTypes.HEADER);
  node=node.getChildren(null)[0];
  checkContainsNodes(node,ManifestTokenType.HEADER_NAME,ManifestTokenType.COLON,ManifestTokenType.SIGNIFICANT_SPACE,ManifestElementTypes.CLAUSE);
  node=node.getChildren(null)[3];
  checkContainsNodes(node,ManifestElementTypes.HEADER_VALUE_PART,ManifestTokenType.SEMICOLON,ManifestElementTypes.ATTRIBUTE);
  node=node.getChildren(null)[2];
  checkContainsNodes(node,ManifestElementTypes.HEADER_VALUE_PART,ManifestTokenType.EQUALS,ManifestElementTypes.HEADER_VALUE_PART);
}
