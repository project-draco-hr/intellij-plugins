{
  PsiBuilder builder=new PsiBuilderImpl(lexer,TokenSet.EMPTY,TokenSet.EMPTY,"Import-Package: ,com.acme\n");
  ASTNode node=testObject.parse(ManifestElementTypes.FILE,builder);
  assertThat(node.getElementType(),sameInstance((IElementType)ManifestElementTypes.FILE));
  checkContainsNodes(node,ManifestElementTypes.SECTION);
  node=node.getChildren(null)[0];
  checkContainsNodes(node,ManifestElementTypes.HEADER);
  node=node.getChildren(null)[0];
  checkContainsNodes(node,ManifestTokenType.HEADER_NAME,ManifestTokenType.COLON,ManifestTokenType.SIGNIFICANT_SPACE,ManifestElementTypes.CLAUSE,ManifestTokenType.COMMA,ManifestElementTypes.CLAUSE);
  checkContainsNodes(node.getChildren(null)[3],ManifestElementTypes.HEADER_VALUE_PART);
  checkContainsNodes(node.getChildren(null)[5],ManifestElementTypes.HEADER_VALUE_PART);
}
