{
  if (myResolveOk) {
    final XmlAttributeDescriptor attributeDescriptor=((JSAttributeImpl)myElement.getParent()).getBackedDescriptor().getAttributeDescriptor(myElement.getName(),null);
    return attributeDescriptor == null ? null : attributeDescriptor.getDeclaration();
  }
  return null;
}
