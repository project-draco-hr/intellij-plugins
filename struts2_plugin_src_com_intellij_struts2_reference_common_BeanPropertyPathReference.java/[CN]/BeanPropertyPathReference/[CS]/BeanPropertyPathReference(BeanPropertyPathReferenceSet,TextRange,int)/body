{
  super(referenceSet.getElement(),range,referenceSet.isSoft());
  ProperTextRange.assertProperRange(range,"invalid range: " + range + " with index "+ index+ " for '"+ referenceSet.getElement().getText()+ "'");
  this.referenceSet=referenceSet;
  this.index=index;
}
