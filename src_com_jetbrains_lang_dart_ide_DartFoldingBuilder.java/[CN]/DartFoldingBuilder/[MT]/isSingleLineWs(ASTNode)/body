{
  if (treeNext != null && treeNext.getElementType() == DartTokenTypesSets.WHITE_SPACE) {
    String text=treeNext.getText();
    if (text.charAt(0) == '\n' && text.indexOf('\n',1) == -1) {
      return true;
    }
  }
  return false;
}
