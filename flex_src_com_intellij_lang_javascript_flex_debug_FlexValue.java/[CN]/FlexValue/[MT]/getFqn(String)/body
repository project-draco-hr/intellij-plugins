{
  if (typeFromFlexValueResult != null && !typeFromFlexValueResult.contains("/")) {
    final int index=typeFromFlexValueResult.indexOf(".<");
    return (index > 0 ? typeFromFlexValueResult.substring(0,index) : typeFromFlexValueResult).replace("::",".");
  }
  return null;
}
