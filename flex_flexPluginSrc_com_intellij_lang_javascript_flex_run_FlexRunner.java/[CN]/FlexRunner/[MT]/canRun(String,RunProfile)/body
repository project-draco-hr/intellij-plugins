{
  return DefaultRunExecutor.EXECUTOR_ID.equals(executorId) && profile instanceof FlexRunConfiguration && ((FlexRunConfiguration)profile).getRunnerParameters() != null && ((FlexRunConfiguration)profile).getRunnerParameters().getRunMode() != FlexRunnerParameters.RunMode.ConnectToRunningFlashPlayer;
}
