{
  FileInfo info=virtualFile.getUserData(INFO);
  if (!isRegistered(info)) {
    if (info == null) {
      info=new FileInfo();
    }
    initInfo(info);
    virtualFile.putUserData(INFO,info);
  }
  return info;
}
