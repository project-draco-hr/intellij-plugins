{
  parents.add(new JSVarStatementStubImpl(parents.getLast()));
  super.processVariable(info,indent,attr);
  String parentName=info.getParentName();
  String qName=getMultinameAsPackageName(info.name,parentName);
  new JSVariableStubImpl(qName.substring(qName.lastIndexOf('.') + 1),info.isConst() ? JSVariableStubImpl.CONST_MASK : 0,getTypeRef(info.type,parentName),getValueRepr(info.value),qName,parents.getLast());
  parents.removeLast();
}
