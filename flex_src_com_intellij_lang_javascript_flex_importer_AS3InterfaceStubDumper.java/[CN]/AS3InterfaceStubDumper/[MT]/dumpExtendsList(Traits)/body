{
  if (!it.base.isStarReference()) {
    String ref=getTypeRef(it.base,null);
    JSReferenceListStub<JSReferenceList> parent=JSStubElementTypes.DEFAULT_EXTENDS_LIST.createStub(parents.getLast());
    new JSReferenceListMemberStubImpl(parent,ref);
  }
}
