{
  parents.add(new JSClassStubImpl(slotInfo.name.name,slotInfo.isInterfaceClass() ? JSClassStubImpl.INTERFACE_MASK : 0,getMultinameAsPackageName(slotInfo.name,null),parents.getLast()));
  super.processClass(slotInfo,abc,attr,indent);
  parents.removeLast();
}
