{
  if (ApplicationManager.getApplication().isUnitTestMode()) {
    return FLEX_UNIT_LAUNCHER;
  }
  return moduleName.replaceAll("[^\\p{Alnum}]","_") + "_" + SystemProperties.getUserName().toLowerCase().replaceAll("[^\\p{Alnum}]","_")+ FLEX_UNIT_LAUNCHER;
}
