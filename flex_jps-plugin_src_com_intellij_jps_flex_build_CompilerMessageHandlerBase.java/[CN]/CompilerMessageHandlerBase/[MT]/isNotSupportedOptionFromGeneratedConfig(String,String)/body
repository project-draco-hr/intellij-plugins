{
  final String fileName=PathUtilRt.getFileName(filePath);
  return fileName.startsWith("idea-") && fileName.endsWith(".xml") && ("'compiler.locale' is not fully supported.".equals(message) || "'compiler.theme' is not fully supported.".equals(message) || "'compiler.preloader' is not fully supported.".equals(message));
}
