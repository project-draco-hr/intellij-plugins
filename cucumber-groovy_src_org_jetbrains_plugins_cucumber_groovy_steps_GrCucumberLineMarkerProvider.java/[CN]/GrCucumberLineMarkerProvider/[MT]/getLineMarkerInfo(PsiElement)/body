{
  if (GrCucumberUtil.isStepDefinition(element)) {
    return new LineMarkerInfo<>(((GrMethodCall)element),element.getTextRange().getStartOffset(),CucumberIcons.Cucumber,Pass.LINE_MARKERS,TOOLTIP_PROVIDER,null);
  }
 else {
    return null;
  }
}
