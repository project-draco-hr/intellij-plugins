{
  if (GrCucumberUtil.isStepDefinition(element)) {
    return new LineMarkerInfo<GrMethodCall>(((GrMethodCall)element),element.getTextRange().getStartOffset(),CucumberIcons.Cucumber,Pass.UPDATE_ALL,TOOLTIP_PROVIDER,null);
  }
 else {
    return null;
  }
}
