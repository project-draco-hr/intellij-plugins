{
  myConsoleMock.expects(once()).method("printHyperlink").with(eq("httP://ww-_w.ru"),NOT_NULL);
  myConsoleMock.expects(once()).method("print").with(eq("\n"),eq(null));
  ConsoleUtil.printMessageIfExists(console(),"httP://ww-_w.ru",null);
}
