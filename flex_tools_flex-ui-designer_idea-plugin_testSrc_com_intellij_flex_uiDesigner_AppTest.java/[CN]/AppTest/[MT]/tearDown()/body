{
  try {
    DesignerApplicationManager.getInstance().dispose();
    StringRegistry.getInstance().reset();
    if (connection != null) {
      connection.disconnect();
    }
  }
  finally {
    super.tearDown();
  }
}
