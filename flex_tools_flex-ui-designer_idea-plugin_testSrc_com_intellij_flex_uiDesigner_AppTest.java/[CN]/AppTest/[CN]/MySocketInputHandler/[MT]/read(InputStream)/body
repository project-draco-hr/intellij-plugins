{
  try {
    createReader(inputStream);
    boolean result=reader.readBoolean();
    if (result) {
      System.out.print("App passed");
    }
 else {
      fail(reader.readUTF());
    }
  }
  finally {
    lock.countDown();
  }
}
