{
  int res=0;
  int i=from;
  final int limit=min(text.length(),toExcluding);
  while (i < limit) {
    i=text.indexOf(c,i);
    if (i >= 0 && i < limit) {
      res++;
      i++;
    }
 else {
      break;
    }
  }
  return res;
}
