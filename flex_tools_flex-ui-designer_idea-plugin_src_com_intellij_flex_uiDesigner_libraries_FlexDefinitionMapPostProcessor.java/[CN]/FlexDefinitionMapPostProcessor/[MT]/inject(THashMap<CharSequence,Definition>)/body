{
  final byte[] injectionData=FileUtil.loadFileBytes(ComplementSwfBuilder.createAbcFile(DebugPathManager.getFudHome() + "/flex-injection/target",version));
  final DataInputStream in=new DataInputStream(new ByteArrayInputStream(injectionData));
  short size=in.readShort();
  definitionMap.ensureCapacity(size);
  in.readShort();
}
