{
  if (methodName == null) {
    return -1;
  }
 else {
    final JSFunction method=clazz.findFunctionByName(methodName);
    if (method == null) {
      return -1;
    }
    return myListModel.indexOf(method);
  }
}
