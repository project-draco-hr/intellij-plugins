{
  String frame=s.frames[0];
  String locationHint=" (" + (frame.length() < 50 ? frame : frame.substring(0,50) + "...") + ")";
  if (Logging.is_debug) {
    locationHint+=" #" + s.id;
  }
  return locationHint;
}
