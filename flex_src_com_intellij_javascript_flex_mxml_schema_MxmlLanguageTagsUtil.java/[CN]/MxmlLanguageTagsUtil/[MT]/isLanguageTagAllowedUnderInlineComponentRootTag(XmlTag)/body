{
  return tag != null && (MXML_URI3.equals(tag.getNamespace()) || MXML_URI.equals(tag.getNamespace())) && ArrayUtil.contains(tag.getLocalName(),LANGUAGE_TAGS_ALLOWED_UNDER_INLINE_COMPONENT_ROOT_TAG);
}
