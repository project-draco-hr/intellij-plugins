{
  if (myServer == null || !sdkHome.equals(mySdkHome)) {
    stopServer();
    startServer(sdkHome);
  }
  if (myServer != null) {
    myRootsHandler.ensureProjectServed(project);
    return true;
  }
  return false;
}
