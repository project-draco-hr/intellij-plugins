{
  if (myServerBusy.get()) {
    try {
synchronized (myServerBusy) {
        if (myServerBusy.get()) {
          myServerBusy.wait();
        }
      }
    }
 catch (    InterruptedException e) {
    }
  }
}
