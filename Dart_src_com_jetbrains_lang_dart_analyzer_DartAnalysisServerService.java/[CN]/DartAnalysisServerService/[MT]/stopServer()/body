{
synchronized (myLock) {
    if (myServer != null) {
      LOG.debug("stopping server");
      myServer.removeAnalysisServerListener(myListener);
      myServer.server_shutdown();
    }
    onServerStopped();
  }
}
