{
  try {
    final Map<String,List<String>> elements=findXMLElements(file.getInputStream(),Arrays.asList("<application><android>","<application><iPhone>"));
    return !elements.get("<application><android>").isEmpty() || !elements.get("<application><iPhone>").isEmpty();
  }
 catch (  IOException e) {
    return false;
  }
}
