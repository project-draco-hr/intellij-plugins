{
  expect(fileSystem.findFileByPath(instanceDefinition.getBaseFolder())).andReturn(root.findChild("eclipse"));
  replay(fileSystem);
  assertThat(testObject.checkValidity(instanceDefinition),equalTo(MessageFormat.format(EquinoxFrameworkInstanceManager.FOLDER_DOES_NOT_EXIST,instanceDefinition.getBaseFolder())));
  verify(fileSystem);
}
