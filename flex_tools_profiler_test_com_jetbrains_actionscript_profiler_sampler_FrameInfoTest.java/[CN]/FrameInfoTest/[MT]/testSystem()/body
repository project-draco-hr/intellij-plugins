{
  final String render="[render]()";
  check(render,null,null,-1,null,"","render",null,null);
  FrameInfoBuilder frameInfoBuilder=new FrameInfoBuilder();
  assertTrue(frameInfoBuilder.buildInstance(render).isSystem());
}
