{
  for (  String testName : testCase.getTests()) {
    File jsTestFile=resolveTestFile(testCase.getFileName());
    TestResultProtocolMessage message=TestResultProtocolMessage.fromDryRun(myJstdConfigFile,browser,jsTestFile,testCase.getName(),testName);
    writeTestResultProtocolMessage(message);
  }
}
