{
  if (obj instanceof ExtractMethodOptions) {
    ExtractMethodOptions other=(ExtractMethodOptions)obj;
    return ObjectUtilities.equals(other.returnType,returnType) && other.createGetter == createGetter && ObjectUtilities.equals(other.name,name) && ObjectUtilities.equals(other.parameters,parameters) && other.extractAll == extractAll;
  }
  return false;
}
