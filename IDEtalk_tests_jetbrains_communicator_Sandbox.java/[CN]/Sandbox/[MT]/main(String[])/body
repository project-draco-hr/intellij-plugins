{
  TimeZone timeZone=TimeZone.getDefault();
  Calendar cal=new GregorianCalendar(timeZone);
  SimpleDateFormat sdv=new SimpleDateFormat();
  sdv.setTimeZone(timeZone);
  String s=sdv.format(new Date());
  System.out.println("s = " + s);
}
