{
  myNames=ContainerUtil.map2Array(sdks,String.class,new Function<Sdk,String>(){
    @Override public String fun(    final Sdk sdk){
      return sdk.getName();
    }
  }
);
  mySdks=sdks;
  init();
}
