{
  myFixture.configureByText(OgnlFileType.INSTANCE,OgnlLanguage.EXPRESSION_PREFIX + " a in <caret>" + OgnlLanguage.EXPRESSION_SUFFIX);
  myFixture.type('{');
  myFixture.checkResult(OgnlLanguage.EXPRESSION_PREFIX + " a in {}" + OgnlLanguage.EXPRESSION_SUFFIX);
}
