{
  myLookupElements=ContainerUtil.map2List(items,new Function<String,LookupElement>(){
    @Override public LookupElementBuilder fun(    String item){
      return LookupElementBuilder.create(item).withCaseSensitivity(false);
    }
  }
);
}
