{
  List<VirtualFile> directories=ContainerUtil.filter(mySdk.getRootProvider().getFiles(type),new Condition<VirtualFile>(){
    @Override public boolean value(    final VirtualFile virtualFile){
      return virtualFile.isDirectory();
    }
  }
);
  return VfsUtil.toVirtualFileArray(directories);
}
