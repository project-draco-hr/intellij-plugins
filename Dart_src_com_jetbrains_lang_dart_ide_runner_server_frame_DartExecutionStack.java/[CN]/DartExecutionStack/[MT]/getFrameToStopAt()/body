{
  return ContainerUtil.find(myStackFrames,new Condition<DartStackFrame>(){
    @Override public boolean value(    final DartStackFrame frame){
      return frame.getSourcePosition() != null;
    }
  }
);
}
