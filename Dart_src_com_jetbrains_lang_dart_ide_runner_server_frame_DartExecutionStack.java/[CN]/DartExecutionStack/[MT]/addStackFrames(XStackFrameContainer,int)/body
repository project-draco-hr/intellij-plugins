{
  final List<DartStackFrame> frames=myStackFrames.size() < 2 ? Collections.<DartStackFrame>emptyList() : myStackFrames.subList(firstFrameIndex,myStackFrames.size());
  container.addStackFrames(frames,true);
}
